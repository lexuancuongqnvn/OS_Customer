@model FilePreviewViewModel

<!-- Display PDF using <embed> tag -->
@if (Model.FileData != null && Model.FileData.Length > 0)
{
    <embed src="data:application/pdf;base64,@Convert.ToBase64String(Model.FileData)" type="application/pdf" style="width:100%;height:100vh;" />
}

